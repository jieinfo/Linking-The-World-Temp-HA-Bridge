# Linking The World Temp HA changelog

## 1.2.0 - Opt-in experimental energy-saving control

- 新增默认关闭的“启用实验性节能控制”选项；只有用户显式启用后，科技系统总控设备才会
  注册“节能控制”写入实体。原有“节能状态”只读实体继续保留。
- 节能控制使用已证实的命令码 `0x06`，接入现有总控串行 latest-value 队列、命令间隔、
  状态查询兜底、超时分级和 Repair；仅匹配主机完整状态回报中的 `energy_saving` 才算确认，
  TCP 写入成功不会被误判为控制成功。
- 自动化测试覆盖制冷、制热、通风、除湿与居家/离家组合下的节能开启回报，且继续拒绝
  未启用实验选项、只读模式、未连接或协议未验证时的写入。本版本仍不实现 `0x60` 批量命令。
- 将总控设备上的“主机温度/主机湿度”显示名称修正为“温度/湿度”；稳定 unique ID、历史
  实体和自动化引用保持不变。数值表示科技系统独立传感器经 Mo-Bus 汇总到主机的上报，
  不再暗示由 MC7021 机身测量。
- 诊断新增实验节能控制是否启用的非机密配置状态，并继续排除地址、凭据、MAC 和原始帧。

## 1.1.0 - Full read-only technology-system status

- 严格解析 MC7021 科技系统总控的 14 字节完整状态块；长度不足、超长或未知布局不再被
  当作有效总控状态，避免字段错位后向 Home Assistant 发布错误测量值。
- 新增节能状态、控制器温度、控制器湿度、PM2.5、CO2、系统故障码和滤网故障码七个
  只读实体。环境测量值使用 Home Assistant 原生单位与测量状态类。
- 系统故障与滤网故障仅公开主机上报的原始整数码：`0` 表示当前报文未报告故障，非零值
  保留原值供诊断；在没有厂商映射证据前不猜测具体故障含义。
- 未知模式或场景值不会阻断同一状态块内其他有效测量值；诊断下载同步包含完整总控只读
  状态，且继续排除地址、凭据、MAC 与原始协议帧。
- 使用授权抓包派生的脱敏真实状态帧增加协议回放，并覆盖短包、长包、未知枚举、非零故障
  码和 Home Assistant 实体端到端测试。本版本没有增加新的写入命令。

## 1.0.3 - HACS-only distribution and release governance

- 仓库现在只维护 HACS 原生集成；移除历史 MQTT Bridge 的源码、附加组件定义、Docker
  构建、同步脚本、示例配置和专用协议测试。历史版本仍可通过 Git 历史查看。
- 发布校验将 `manifest.json`、CHANGELOG 首节、README 当前版本和正式 `vX.Y.Z` 标签
  视为同一个版本事实；任一不一致都会阻止 CI 通过。
- 在干净 CI 运行器中保留 HACS 校验，并以全新 Home Assistant 配置条目加载/重载作为
  发布前安装升级烟测，避免主分支留下旧实现或不完整的发布元数据。

## 1.0.2 - Total-control command coordination and diagnostics

- 六恒总开关、模式、场景和冬季加湿统一使用串行 latest-value 命令队列；上一条命令等待
  主机确认时，同类连续操作只保留最后意图，不同类型操作仍按用户顺序执行。
- 模式和冬季加湿在真正写入 TCP 前重新检查主机确认状态，避免排队期间状态变化后越过
  联锁；检查尊重队列先后顺序，后续意图不会反向阻止更早的合法命令。
- 快速反向操作回到主机已经确认的状态时，移除同类旧队列值并跳过冗余发包，避免不产生
  新状态上报的重复命令占用确认窗口。
- 科技系统运行期间不再把模式实体标记为不可用；实体保持在线并只显示当前模式，关闭总控
  后恢复制冷、制热、通风和除湿四个可选项。
- 诊断新增当前/峰值命令队列深度、连续最终确认超时和超时后恢复次数，便于定位会话拥塞与
  主机状态确认异常。
- 被更新意图取代的中间命令超时降为 `INFO`；最终命令首次和第二次超时使用 `WARNING`，
  连续第三次起使用 `ERROR` 并创建 Home Assistant Repair。下一条命令确认后自动清除。
- 在 Home Assistant `2026.8.2` 与真实 MC7021 上验证 `1.0.1` 的总控模式链路：总控关闭时
  连续执行 20 次模式切换均由主机主动上报确认，确认耗时约 1.06-2.15 秒，未发生兜底查询、
  重试、最终超时或相关错误日志。

## 1.0.1 - Official client behavior alignment

- 命令发送后优先等待 MC7021 主动状态上报；默认 1 秒内未确认时，才发送状态查询作为兜底。
- 多条待确认命令共享一次状态查询，保留后续轮询、8 秒确认超时和温度命令单次重试。
- 登录后的初始化查询之间增加 150ms 间隔，降低旧固件及多客户端并发时的瞬时压力。
- 诊断新增主动上报确认、查询后确认和兜底查询计数，便于区分主机推送延迟与控制失败。
- 新增由授权抓包派生的脱敏官方中控屏协议帧回归测试，不保存地址、账号或家庭标识。

## 1.0.0 - Native integration stable release preparation

- 将原生 HACS 集成版本设为 `1.0.0`，最低 Home Assistant 版本设为 `2026.8.0`。
- 使用真实 Home Assistant pytest 运行时及可编程 MC7021 服务器覆盖配置、加载、卸载、
  重载、重连、实体、动态面板、命令确认、协议分帧和压力场景；CI 对最低 2026.8 基线和
  当前受支持运行时执行 95% 覆盖率门槛。两项运行时均使用 Python `3.14` 和上游要求的
  `pytest-cov==7.1.0`。
- 连接生命周期现在明确区分未连接、连接、握手、认证和就绪；仅 `READY` 且状态流已验证
  时报告控制器在线。TCP、握手、登录超时、明确认证拒绝、协议不兼容和状态静默均使用
  独立的诊断失败类别。
- 初次配置中，捕获到的明确错误密码响应立即显示 `invalid_auth`；已经加载的配置项遇到
  同一响应会启动条目内重新认证并停止自动凭据重试。超时、EOF、套接字关闭和未知响应
  不会被猜测为坏密码。
- 新增可操作的 Home Assistant Repairs：第三次连续登录超时、协议不兼容，以及每个达到
  阈值的长期未上报面板都有独立的恢复流程；成功认证或有效面板报告会清除相应问题。
- 面板存储升级为版本化持久化。升级的旧记录从零已观察缺席时间开始，避免升级后立即
  产生删除提示。只有 `READY` 状态下可证明活跃的有效状态流才累积 30 天缺席；面板绝不
  自动删除，必须在 Repair 中明确确认。
- 诊断导出和健康历史使用有界、匿名化数据，移除地址、凭据、客户端 ID、MAC、房间名、
  密钥、令牌和原始协议帧；包括 debug 模式在内，生产日志只输出协议元数据，不输出原始帧。
- 房间面板开关命令在最近一次有效上报已经等于目标状态时直接完成，不再向 MC7021 发送
  不会产生新状态回报的重复命令，避免总控恢复后重复开启面板造成错误的确认超时。
- 同一面板的开关命令尚待确认时，相反方向的新操作进入 latest-value 队列；首个命令确认
  或超时后继续执行最后操作，不会因最近已确认状态恰好相同而误判完成、丢失用户意图。
- 面板命令队列按电源和设定温度分别保留最新意图，同时继续按面板串行发送；两类操作不再
  互相覆盖。排队的开启命令携带安全门，并在实际发送前重新检查总控与运行模式。
- 修复上一温度刚确认、旧队列值尚未派发时新温度先发送，随后旧值反向覆盖最终设定的竞态；
  新输入现在会在同一命令锁内清除尚未派发的旧值，确保最后一次调温最终生效。
- 补充 HACS 安装、迁移、故障恢复、隐私和现场验证文档。本文档不是现场验证记录；未知
  MC7021 固件和协议结构仍须在目标环境按发布核对表确认。

## 0.1.7

- 对齐原版科技系统逻辑：房间温控面板仅在科技系统已开启且处于制冷或制热模式时允许运行。
- 通风、除湿模式下，房间 Climate 实体保持在线并继续显示温湿度，但运行状态固定为关闭。
- 总控关闭、通风或除湿模式下不再提供房间面板开启选项；底层在发包前再次校验并保留明确错误原因，不再向 MC7021 发送无效命令或等待确认超时。

## 0.1.6

- 过滤 MC7021 在频繁调温期间短暂上报的 `100°C / 100%` 无效测量占位值。
- 异常测量期间保留面板最后一次可信温湿度，不再让 Climate 实体瞬时跳到 100。
- 无效测量不进入自动化中位数滤波，避免自动化温湿度传感器被连续占位值污染。
- 状态包中的设定温度、开关、在线时间和命令确认仍正常处理。

## 0.1.5

- 将原生集成的连续调温改为与附加组件一致的保守串行策略。
- 同一面板上一条命令确认前只保留最后一次温度，不再提前发送中间或替代命令。
- 仅在上一条命令确认成功或确认窗口结束后发送最终温度，降低 MC7021 命令积压和状态交错概率。
- 未发送的排队温度不再被旧状态回报误判为已确认。

## 0.1.4

- 房间温度最终命令 8 秒未确认时自动重发一次，并继续主动查询 MC7021 状态。
- 默认最多等待两个确认周期；第二次仍未确认才记录最终错误并释放命令。
- 自动重试仅用于幂等的房间温度设定，不应用于总开关、模式、场景或面板开关。
- 最终超时日志增加发送次数，便于区分首次状态延迟和两次均未响应。

## 0.1.3

- 修复长期 MC7021 TCP 会话被 Home Assistant 当作前台启动任务，导致启动阶段等待约 5 分钟的问题。
- 使用配置条目管理的后台任务生命周期，卸载、重载和关机时由 Home Assistant 自动取消，并继续执行显式断连清理。

## 0.1.2

- 优化房间温控面板快速连续调温，只保留并确认用户最后选择的温度。
- 重复温度命令自动去重；中间温度停止变化约 0.75 秒后推进最终值，连续操作最长等待 3 秒。
- 命令等待确认期间主动补查 MC7021 状态，减少状态回报延迟造成的误超时。
- 扩充确认超时日志上下文，记录目标、期望状态及等待时间。

## 0.1.1

- 修复 Home Assistant 2026.8 中配置向导无法加载并返回 500 的问题。
- 将配置表单改为前端可序列化的标准字段，并在提交后继续严格校验主机地址、客户端 ID 与总控 MAC。

## 0.1.0

- 新增可通过 HACS 安装的原生 Home Assistant 集成。
- 使用异步 TCP/9000 直接连接 MC7021，不依赖 MQTT 或 Mosquitto。
- 原生提供科技系统总开关、中文模式/场景、冬季加湿和房间 Climate 实体。
- 按主机状态上报动态发现任意数量的房间温控面板，并持久保存设备清单。
- 提供平滑温湿度、连接状态、协议验证、控制权限、最近命令和面板数量诊断实体。
